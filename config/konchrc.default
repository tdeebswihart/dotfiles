import konch

import os
import sys
from pprint import pprint as pp

konch.config({
    'context': {
        'os': os,
        'sys': sys,
        'pp': pp,
        'pjoin': os.path.join
    },
    'shell': 'ptipython'
})

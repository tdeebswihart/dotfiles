#!/bin/bash
if test $# -lt 1 ; then
    echo "usage: n [program]"
    for i in $(ls ~/bin/nodejs/); do
        test -f ~/bin/nodejs/$i && echo "- ${i%.*}"
    done
    exit 2
else
    node ~/bin/nodejs/$1.js
fi
